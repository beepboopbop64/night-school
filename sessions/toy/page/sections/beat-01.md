# beat-01: The aha, trued

> A lesson pipeline is a build system whose artifacts happen to teach.

Every artifact on this page was written by a deterministic node, hashed into a
receipt, and committed with its cost in the message. Change the storyboard and
only the beats it touches rebuild. The unit of incremental rebuild is the
beat, never the session.

This section is toy prose: the walking skeleton proves the rails, not the
teaching. Real sections arrive with the Showrunner at M5.
